version: '0.1'

services:
  db:
    image: postgres
    ports:
      - 5462:5432
    volumes:
      - ./data/db:/var/lib/postgresql/data
    env_file:
      - .env

  rabbitmq_server:
    image: rabbitmq:3-management
    ports:
      - 5672:5672
      - 15672:15672

  worker:
    build:
      context: .
      dockerfile: worker.Dockerfile
    depends_on: 
      - rabbitmq_server

  backend:
    build: ./
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    env_file:
      - .env
    depends_on:
      - db
      - worker